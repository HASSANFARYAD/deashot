/** Server tick rate in Hz. */
export const SERVER_TICK_RATE = 60;

/** Client input send rate in Hz. */
export const CLIENT_INPUT_RATE = 30;

/** State broadcast rate in Hz. */
export const SERVER_SNAPSHOT_RATE = 20;

/** Maximum players per TDM room. */
export const MAX_PLAYERS = 8;

/** Default match duration in seconds. */
export const MATCH_DURATION = 600;

/** Team kill limit to win. */
export const KILL_LIMIT = 50;
